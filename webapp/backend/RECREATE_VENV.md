# 🔄 Пересоздание виртуального окружения с Python 3.12

## Автоматический способ (рекомендуется)

### Вариант 1: Использовать bash скрипт

```bash
cd webapp/backend
chmod +x recreate_venv.sh
./recreate_venv.sh
```

### Вариант 2: Использовать Python скрипт

```bash
cd webapp/backend
python3 recreate_venv.py
```

## Ручной способ

Если автоматические скрипты не работают, выполните команды вручную:

```bash
cd webapp/backend

# 1. Удалите старое venv
rm -rf venv

# 2. Создайте новое с Python 3.12
/opt/homebrew/opt/python@3.12/bin/python3.12 -m venv venv
# Или если python3.12 в PATH:
python3.12 -m venv venv

# 3. Активируйте
source venv/bin/activate

# 4. Проверьте версию (должно быть 3.12.x)
python --version

# 5. Обновите pip
pip install --upgrade pip setuptools wheel

# 6. Установите зависимости
pip install -r requirements.txt
```

## Проверка после пересоздания

```bash
source venv/bin/activate
python --version  # Должно быть Python 3.12.x
which python      # Должен указывать на venv/bin/python
pip list          # Проверьте установленные пакеты
```

## Если Python 3.12 не найден

Установите через Homebrew:

```bash
brew install python@3.12
```

Затем повторите процесс пересоздания.

