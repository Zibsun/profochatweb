# 🚀 Быстрый запуск тестовой страницы

## Вариант 1: Только Frontend (самый быстрый способ)

Этот вариант запустит только frontend без backend. Подойдет для проверки UI компонентов.

```bash
cd webapp/frontend
npm install
npm run dev
```

Откройте в браузере: **http://localhost:3000/test**

---

## Вариант 2: Frontend + Backend (полный запуск)

### Шаг 1: Запуск Backend

В первом терминале:

```bash
cd webapp/backend

# Создайте виртуальное окружение (если еще не создано)
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env
# Отредактируйте .env и укажите хотя бы SECRET_KEY (можно любой случайный)

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

### Шаг 2: Запуск Frontend

Во втором терминале:

```bash
cd webapp/frontend
npm install
npm run dev
```

### Шаг 3: Откройте тестовую страницу

**http://localhost:3000/test**

---

## Вариант 3: Docker Compose (все сервисы)

```bash
cd webapp

# Создайте .env файл для backend
cp backend/.env.example backend/.env
# Отредактируйте backend/.env

# Запустите все сервисы
docker-compose up -d

# Откройте http://localhost:3000/test
```

---

## Что проверить на тестовой странице

1. ✅ Кнопки разных типов работают
2. ✅ Прогресс-бар обновляется при нажатии
3. ✅ Индикатор загрузки отображается
4. ✅ Карточки отображаются корректно
5. ✅ Кнопка "Проверить Backend API" работает (если backend запущен)

---

## Решение проблем

### Ошибка "Cannot find module"
```bash
cd webapp/frontend
rm -rf node_modules package-lock.json
npm install
```

### Порт 3000 занят
Измените порт в `package.json`:
```json
"dev": "next dev -p 3001"
```

### Backend не запускается
Проверьте:
- Python версия 3.12+
- Все зависимости установлены: `pip install -r requirements.txt`
- Файл `.env` существует и заполнен

---

## Полезные ссылки

- Frontend: http://localhost:3000
- Тестовая страница: http://localhost:3000/test
- Backend API: http://localhost:8000
- API документация: http://localhost:8000/docs

