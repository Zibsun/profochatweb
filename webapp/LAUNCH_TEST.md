# 🎯 Запуск тестовой страницы - Пошаговая инструкция

## ✅ Что уже готово

1. ✅ Тестовая страница создана: `webapp/frontend/app/test/page.tsx`
2. ✅ Все компоненты UI готовы
3. ✅ Структура проекта настроена

## 🚀 Шаг 1: Установка зависимостей Frontend

Откройте терминал и выполните:

```bash
cd webapp/frontend
    npm install
```

Это установит все необходимые пакеты (Next.js, React, Tailwind CSS и др.)

## 🚀 Шаг 2: Запуск Frontend

После установки зависимостей запустите dev сервер:

```bash
npm run dev
```

Вы увидите сообщение:
```
▲ Next.js 14.x.x
- Local:        http://localhost:3000
```

## 🌐 Шаг 3: Откройте тестовую страницу

Откройте в браузере:

**http://localhost:3000/test**

## ✨ Что вы увидите на тестовой странице

1. **Компоненты UI** - кнопки разных типов, прогресс-бар, индикатор загрузки
2. **Интерактивные элементы** - можно нажимать кнопки и видеть изменения
3. **Тест API** - кнопка для проверки подключения к backend (работает если backend запущен)

## 🔧 Опционально: Запуск Backend

Если хотите протестировать полную интеграцию:

### В новом терминале:

```bash
cd webapp/backend

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate

# Установите зависимости
pip install -r requirements.txt

# Создайте .env файл
cp .env.example .env

# Отредактируйте .env и укажите минимум:
# SECRET_KEY=любой-случайный-ключ
# DATABASE_URL=postgresql://user:pass@localhost:5432/dbname

# Запустите сервер
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

Backend будет доступен на: **http://localhost:8000**

## 🐛 Решение проблем

### Ошибка при `npm install`
```bash
# Очистите кэш и попробуйте снова
rm -rf node_modules package-lock.json
npm cache clean --force
npm install
```

### Порт 3000 занят
Измените порт в команде запуска:
```bash
npm run dev -- -p 3001
```

### Ошибка "Module not found"
Убедитесь, что выполнили `npm install` в директории `webapp/frontend`

### Страница не открывается
1. Проверьте, что сервер запущен (должно быть сообщение в терминале)
2. Проверьте URL: должен быть `http://localhost:3000/test` (не `/test` без порта)
3. Проверьте консоль браузера на ошибки (F12)

## 📝 Быстрые команды

```bash
# Перейти в frontend
cd webapp/frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm run dev

# Остановить сервер
Ctrl+C
```

## 🎉 Готово!

После выполнения этих шагов тестовая страница будет доступна по адресу:
**http://localhost:3000/test**

