# Конфигурация

Справочник по конфигурации системы.

## Документы

- [config.yaml](config-yaml.md) — структура config.yaml
- [Переменные окружения](environment-variables.md) — переменные окружения

## Основные файлы конфигурации

- `config.yaml` — основной файл конфигурации
- `.env` — переменные окружения (backend)
- `.env.local` — переменные окружения (frontend)
