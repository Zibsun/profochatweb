# Документация по Elements

Эта документация описывает все типы элементов, которые можно использовать при создании курсов в формате YAML.

## Содержание

- [Общие параметры](#общие-параметры)
- [Типы элементов](#типы-элементов)
  - [Message](#message) - Простое сообщение
  - [Audio](#audio) - Аудиосообщение
  - [Input](#input) - Ввод текста пользователем
  - [Quiz](#quiz) - Викторина с одним правильным ответом
  - [Question](#question) - Опрос без правильного ответа
  - [MultiChoice](#multichoice) - Множественный выбор с несколькими правильными ответами
  - [Dialog](#dialog) - Диалог с ИИ
  - [Miniapp](#miniapp) - Мини-приложение Telegram
  - [Test](#test) - Итоговый тест с подсчетом баллов
  - [Jump](#jump) - Навигация с опциями
  - [Revision](#revision) - Повторение ошибок
  - [Delay](#delay) - Задержка перед следующим элементом
  - [End](#end) - Завершение курса
  - [Section](#section) - Разделитель секций (только для редактора)

---

## Общие параметры

Все элементы поддерживают следующие общие параметры:

### `parse_mode` (опционально)
Режим форматирования текста:
- `HTML` - HTML форматирование
- `MARKDOWN` (по умолчанию) - Markdown форматирование
- `HTML!` - HTML форматирование (только для Dialog, влияет на ответы модели)

**Пример:**
```yaml
MyMessage:
  type: message
  parse_mode: HTML
  text: >
    <b>Жирный текст</b> и <i>курсив</i>
```

### `link_preview` (опционально)
Показывать ли превью ссылок в сообщении:
- `yes` - показывать превью (по умолчанию для большинства элементов)
- `no` - не показывать превью (по умолчанию для Message с кнопкой и Dialog)

### `media` (опционально)
Массив URL медиафайлов для отправки вместе с сообщением. Поддерживаются:
- Изображения (JPG, PNG и др.) - для элемента `message`
- Видео (MP4) - для элемента `message`
- Аудио (MP3, M4A, OGG) - для элемента `audio` (обязательный параметр)

**Пример:**
```yaml
MyMessage:
  type: message
  text: Посмотри на эти картинки
  media:
    - https://example.com/image1.jpg
    - https://example.com/image2.jpg
```

---

## Типы элементов

### Message

Простое текстовое сообщение от бота. Может содержать кнопку для продолжения.

**Параметры:**
- `type`: `message` (обязательно)
- `text` (обязательно) - текст сообщения
- `button` (опционально) - текст кнопки для продолжения. Если указан, элемент будет ждать нажатия кнопки
- `parse_mode` (опционально) - режим форматирования
- `link_preview` (опционально) - показывать ли превью ссылок
- `media` (опционально) - массив URL изображений/видео

**Поведение:**
- Если указан `button`, элемент ждет нажатия кнопки перед переходом к следующему
- Если `button` не указан, элемент автоматически переходит к следующему

**Примеры:**

```yaml
# Простое сообщение без кнопки
Course_intro_00:
  type: message
  text: >
    Привет! Рад тебя видеть! Я — 🤖 Greek Bot!
```

```yaml
# Сообщение с кнопкой
Course_intro_01:
  type: message
  text: >
    Давай проверим несколько вопросов!
  button: Давай
```

```yaml
# Сообщение с медиа и HTML форматированием
Message_Decrease:
  type: message
  parse_mode: HTML
  text: >
    💡 <b>Increase</b>
    
    <b>Definition</b>: To become larger or greater.
    
    <tg-spoiler>
    <b>Определение</b>: Стать больше.
    </tg-spoiler>
  button: Поехали!
  media:
    - https://drive.google.com/file/d/1abc123/view
```

---

### Audio

Элемент для отправки аудиосообщения в Telegram с сопровождающим текстом. Используется для упражнений на аудирование, диалогов, примеров произношения.

**Параметры:**
- `type`: `audio` (обязательно)
- `media` (обязательно) - массив URL аудиофайлов (обычно один файл)
- `text` (опционально) - текст перед или вместе с аудио
- `parse_mode` (опционально) - режим форматирования текста
- `link_preview` (опционально) - показывать ли превью ссылок

**Поведение:**
- Отправляет аудиофайл(ы) в Telegram
- Если указан `text`, отправляет его как caption к аудио (или отдельным сообщением, если `link_preview: no`)
- Элемент не ожидает ответа пользователя и автоматически переходит к следующему
- Кнопки не поддерживаются
- Не оценивается

**Поддерживаемые форматы аудио:**
- MP3
- M4A
- OGG

**Пример:**

```yaml
Listening_Audio_02:
  type: audio
  text: |
    🎧 Прослушай аудио.
    В нём описаны 6 ситуаций.
    
    Соотнеси каждое описание с соответствующей картинкой.
  media:
    - https://drive.google.com/uc?export=download&id=1_IQ4i20UoKYFRjMPIk7jorC8g81mUS-M
```

```yaml
# Аудио без текста
Audio_Only:
  type: audio
  media:
    - https://example.com/audio.mp3
```

```yaml
# Аудио с отключенным link_preview для текста
Audio_NoPreview:
  type: audio
  text: |
    Прослушай инструкцию.
    Ссылка: https://example.com
  link_preview: no
  media:
    - https://example.com/instruction.mp3
```

---

### Input

Элемент для получения текстового ответа от пользователя. Может проверять правильность ответа.

**Параметры:**
- `type`: `input` (обязательно)
- `text` (обязательно) - вопрос или инструкция для пользователя
- `correct_answer` (опционально) - правильный ответ для проверки
- `feedback_correct` (опционально) - сообщение при правильном ответе
- `feedback_incorrect` (опционально) - сообщение при неправильном ответе
- `input_type` (опционально) - тип нормализации ввода:
  - `text` (по умолчанию) - стандартное текстовое сравнение (регистронезависимое)
  - `sequence` - нормализация последовательностей цифр (игнорирует разделители: пробелы, запятые и т.д.)

**Поведение:**
- Пользователь вводит текстовый ответ
- Если указан `correct_answer`, ответ проверяется:
  - Для `input_type: text` - регистронезависимое сравнение строк
  - Для `input_type: sequence` - нормализация последовательностей цифр (убираются все разделители перед сравнением)
- В зависимости от результата отправляется соответствующий feedback
- Результат сохраняется в отчет с оценкой (1 или 0)

**Примеры:**

```yaml
# Стандартное текстовое сравнение
Question_01:
  type: input
  text: |
    Кто президент США?
  correct_answer: Байден
  feedback_correct: Правильно!
  feedback_incorrect: Неправильно. Правильный ответ: Байден
```

```yaml
# Последовательность цифр (разделители игнорируются)
Listening_Input_03:
  type: input
  input_type: sequence
  text: |
    ✏️ Задание
    
    Соотнеси описания из аудио с картинками.
    
    Напиши номера картинок
    в том порядке, как они идут в аудио.
    
    Можно использовать любые разделители.
    
    Пример:
    3 1 6 2 4 5
  correct_answer: "3 1 6 2 4 5"  # или "3,1,6,2,4,5" - оба варианта будут правильными
  feedback_correct: |
    ✅ Отлично!
    Ты правильно сопоставил все описания.
  feedback_incorrect: |
    ❌ Почти получилось.
    Проверь порядок и переслушай аудио.
```

**Примечание:** При `input_type: sequence` все нецифровые символы (пробелы, запятые, точки с запятой и т.д.) игнорируются при сравнении. Например, ответы `"1,2,3"`, `"1 2 3"`, `"1, 2, 3"` и `"123"` будут считаться одинаковыми.

---

### Quiz

Викторина с одним правильным ответом. Отправляется как Telegram Poll типа "quiz".

**Параметры:**
- `type`: `quiz` (обязательно)
- `text` (обязательно) - вопрос викторины (максимум 300 символов)
- `answers` (обязательно) - массив вариантов ответов:
  - `text` (обязательно) - текст варианта ответа
  - `correct` (опционально) - `yes` для правильного ответа
  - `feedback` (опционально) - сообщение после выбора этого варианта
- `media` (опционально) - массив URL изображений (отправляются перед викториной)

**Поведение:**
- Отправляется как Telegram Poll с типом "quiz"
- Один из вариантов должен быть помечен как правильный (`correct: yes`)
- После выбора варианта отправляется соответствующий `feedback`
- Результат сохраняется в отчет с оценкой (1 за правильный ответ, 0 за неправильный)

**Пример:**

```yaml
Vocab_1:
  type: quiz
  text: Important (1/50)
  answers:
    - text: Viral
      feedback: Almost there, let's try again!
    - text: Essential
      correct: yes
      feedback: Awesome job!
```

```yaml
# Quiz с изображением
Graph_Decrease:
  type: quiz
  text: The sales figures increased steadily according to the graph.
  media:
    - https://www.quickchart.io/chart?c=...
  answers:
    - text: 'TRUE'
      correct: yes
      feedback: Great job!
    - text: 'FALSE'
      feedback: Oops! That's not correct.
```

---

### Question

Опрос без правильного ответа. Отправляется как обычный Telegram Poll.

**Параметры:**
- `type`: `question` (обязательно)
- `text` (обязательно) - вопрос опроса (максимум 300 символов)
- `answers` (обязательно) - массив вариантов ответов:
  - `text` (обязательно) - текст варианта ответа
  - `feedback` (опционально) - сообщение после выбора этого варианта

**Поведение:**
- Отправляется как Telegram Poll типа "regular" (не quiz)
- Нет правильного ответа - все варианты равнозначны
- После выбора варианта отправляется соответствующий `feedback` (если указан)
- Результат сохраняется в отчет без оценки

**Пример:**

```yaml
Course_intro_02:
  type: question
  text: |
    Делаешь ли ты что-то на уроках для создания комфортной среды?
  answers:
    - text: Да.
      feedback: Ты молодец, это очень важно.
    - text: Нет, не думал про это.
      feedback: Тогда этот курс точно для тебя!
```

---

### MultiChoice

Множественный выбор с несколькими правильными ответами. Пользователь может выбрать несколько вариантов.

**Параметры:**
- `type`: `multi_choice` (обязательно)
- `text` (обязательно) - вопрос (максимум 300 символов)
- `answers` (обязательно) - массив вариантов ответов:
  - `text` (обязательно) - текст варианта ответа
  - `correct` (опционально) - `yes` для правильного, `no` для неправильного
  - `feedback` (опционально) - сообщение для этого варианта
- `feedback_correct` (обязательно) - сообщение при полностью правильном ответе
- `feedback_partial` (обязательно) - сообщение при частично правильном ответе
- `feedback_incorrect` (обязательно) - сообщение при неправильном ответе

**Поведение:**
- Отправляется как Telegram Poll с `allows_multiple_answers = True`
- Пользователь может выбрать несколько вариантов
- После выбора показывается детальный feedback по каждому выбранному варианту
- Затем показывается итоговое сообщение в зависимости от результата:
  - `feedback_correct` - если все правильные выбраны и все неправильные не выбраны
  - `feedback_partial` - если есть частично правильные ответы
  - `feedback_incorrect` - если все ответы неправильные
- Оценка: 1 (полностью правильно), 0.5 (частично), 0 (неправильно)

**Пример:**

```yaml
INVEST_question:
  type: multi_choice
  text: Выбери те критерии INVEST, которым она соответствует.
  feedback_correct: >
    Итог: отлично, все абсолютно верно!
  feedback_partial: >
    Итог: почти верно, но есть одна неточность.
  feedback_incorrect: >
    Эх, эту тему ты пока не знаешь.
  answers:
    - text: Независимая (Independent)
      correct: no
      feedback: >
        Нет, эта история не является независимой.
    - text: Обсуждаемая (Negotiable)
      correct: yes
      feedback: >
        Да! История обсуждаемая.
    - text: Оцениваемая (Estimable)
      correct: no
      feedback: >
        Нет, история не оцениваемая.
    - text: Тестируемая (Testable)
      correct: yes
      feedback: >
        Да! История тестируемая.
```

---

### Dialog

Диалог с ИИ-ассистентом. Позволяет пользователю общаться с ботом в свободной форме.

**Параметры:**
- `type`: `dialog` (обязательно)
- `text` (обязательно) - начальное сообщение от бота
- `prompt` (обязательно) - системный промпт для ИИ-модели
- `model` (опционально) - модель ИИ для использования
- `temperature` (опционально) - температура для генерации (0-1)
- `reasoning` (опционально) - параметр reasoning для модели
- `conversation` (опционально) - начальная история диалога
- `parse_mode` (опционально) - режим форматирования (`HTML!` для HTML в ответах модели)
- `link_preview` (опционально) - показывать ли превью ссылок
- `transcription_language` (опционально) - язык для транскрибации голосовых сообщений (ISO-639-1 код, например `el` для греческого). Если не указан, язык определяется автоматически с использованием Eleven Labs Scribe API
- `voice_response` (опционально) - включить голосовые ответы (`true`/`false`, по умолчанию `false`). Если включено, ответы бота будут отправляться как голосовые сообщения с использованием Eleven Labs TTS API
- `auto_start` (опционально) - автоматически начать диалог (`true`/`false`, по умолчанию `false`). Если `true`, бот сам отправит первое сообщение после начального текста, не ожидая ответа пользователя
- `tts_voice` (опционально) - ID голоса Eleven Labs для синтеза речи (например, `21m00Tcm4TlvDq8ikWAM`). По умолчанию используется `21m00Tcm4TlvDq8ikWAM`. Старые значения OpenAI (`alloy`, `echo`, `fable`, `onyx`, `nova`, `shimmer`) автоматически преобразуются в соответствующие ID Eleven Labs
- `tts_model` (опционально) - модель Eleven Labs TTS: `eleven_multilingual_v2` (по умолчанию), `eleven_turbo_v2`, `eleven_monolingual_v1`. Старые значения OpenAI (`tts-1`, `tts-1-hd`) автоматически преобразуются в `eleven_multilingual_v2`
- `tts_speed` (опционально) - скорость речи: от `0.25` (медленно) до `4.0` (быстро), по умолчанию `1.0`. Рекомендуется `0.8-0.9` для более медленной и понятной речи

**Особенности:**
- Поддерживает переменные в промпте: `{{element_id}}` - подставляет текст и историю диалога из указанного элемента
- Формат переменных:
  - `{{N]element_id}}` - первые N сообщений из элемента
  - `{{element_id[M}}` - последние M сообщений из элемента
- Диалог продолжается до тех пор, пока модель не ответит `{STOP}` или `#конецдиалога`
- Показывает индикатор "печатает..." во время генерации ответа
- **Поддержка голосовых сообщений (входящих):**
  - Если пользователь отправляет голосовое сообщение, оно автоматически транскрибируется в текст с использованием Eleven Labs Scribe API
  - Транскрибированный текст отправляется пользователю в виде сообщения "🎤 Распознано: {текст}" перед обработкой диалогом
  - Затем транскрибированный текст передается в диалог как обычное текстовое сообщение
  - Язык транскрибации можно указать через параметр `transcription_language` (ISO-639-1 код, например `el` для греческого)
  - Если язык не указан, Scribe автоматически определяет язык голосового сообщения
- **Поддержка голосовых ответов (исходящих):**
  - Если включен параметр `voice_response: true`, ответы бота генерируются как голосовые сообщения с использованием Eleven Labs TTS API
  - Голосовое сообщение отправляется с текстом ответа в качестве caption (до 1024 символов), текст в caption скрыт под спойлером `<tg-spoiler>`
  - Можно выбрать голос через параметр `tts_voice` (ID голоса Eleven Labs) и модель через `tts_model`
  - Если генерация голоса не удалась, автоматически отправляется текстовое сообщение (fallback)
- **Автоматический старт диалога:**
  - Если включен параметр `auto_start: true`, бот автоматически отправит первое сообщение после начального текста (`text`)
  - Это полезно для диалогов, где бот должен сам инициировать разговор (например, задать первый вопрос)
  - Первое сообщение будет голосовым, если включен `voice_response: true`

**Пример:**

```yaml
Taboo_lesson_08:
  type: dialog
  text: >
    Давай обсудим. Как ты понял(а), почему задания не подходят для первого урока?
  prompt: >
    # Persona
    
    Your role is a friendly teacher who created the course.
    You are talking with a user who has just started the course.
    
    # Goals
    
    - to encourage the user to learn this course
    - to answer questions about the course's content
    
    # Context
    
    {{Taboo_lesson_02}}
    
    Use the information above to help the user.
  model: gpt-4
  temperature: 0.7
```

```yaml
# Dialog с поддержкой голосовых сообщений на греческом языке
Writing_Dialog_01:
  type: dialog
  text: |
    Теперь давай напишем письмо на греческом языке по этой теме!
    Можешь писать фразу за фразой или говорить голосовыми сообщениями.
  prompt: |
    Ты — доброжелательный преподаватель греческого языка.
    Ученик может писать или говорить на греческом языке.
  transcription_language: el  # Греческий язык для транскрибации голосовых сообщений
  model: gpt-4
  temperature: 0.7
```

```yaml
# Dialog с голосовыми ответами и автоматическим стартом
Speaking_Dialog_01:
  type: dialog
  text: |
    Давай потренируемся говорить на греческом языке!
    Я буду отвечать голосом, чтобы ты мог слышать правильное произношение.
  prompt: |
    Ты — преподаватель греческого языка.
    Помогай ученику практиковать разговорную речь.
    Начни диалог с приветствия на греческом языке.
  voice_response: true  # Включить голосовые ответы
  auto_start: true  # Бот сам начнет диалог после начального сообщения
  tts_voice: 21m00Tcm4TlvDq8ikWAM  # ID голоса Eleven Labs (можно использовать старые значения: nova, alloy и т.д.)
  tts_model: eleven_multilingual_v2  # Модель Eleven Labs TTS (можно использовать старые значения: tts-1-hd)
  tts_speed: 0.8  # Скорость речи (рекомендуется 0.8-0.9 для более медленной речи)
  transcription_language: el  # Язык для транскрибации входящих голосовых сообщений
  model: gpt-4
  temperature: 0.7
```

---

### Miniapp

Открывает Telegram Mini App (веб-приложение) для интерактивного взаимодействия.

**Параметры:**
- `type`: `miniapp` (обязательно)
- `text` (обязательно) - сообщение перед открытием мини-приложения
- `button` (обязательно) - текст кнопки для открытия мини-приложения
- `app_url` (обязательно) - URL мини-приложения (к нему будет добавлен ID вопроса)
- `question` (обязательно) - описание вопроса/задания (используется мини-приложением)

**Поведение:**
- Отправляет сообщение с кнопкой
- При нажатии открывается Telegram Mini App
- В URL передается ID вопроса для связи с ответом пользователя

**Пример:**

```yaml
Mini_App_Test_01:
  type: miniapp
  text: |
    Mini App launch Test
  button: Launch
  app_url: https://example.com/miniapp
  question:
    type: multiple_choice
    text: |
      What is the capital of France?
    options:
      - text: Paris
      - text: London
      - text: Berlin
      - text: Madrid
```

---

### Test

Итоговый тест с подсчетом баллов по всем элементам с указанным префиксом.

**Параметры:**
- `type`: `test` (обязательно)
- `text` (обязательно) - текст результата. Поддерживает переменные:
  - `{score}` - набранные баллы
  - `{maxscore}` - максимальные баллы
- `prefix` (обязательно) - префикс ID элементов для подсчета (например, `q_` для элементов `q_1`, `q_2`, etc.)
- `score` (обязательно) - словарь с оценками по проценту ошибок:
  - Ключ - максимальный процент ошибок (число)
  - Значение - сообщение для этого диапазона
- `button` (опционально) - текст кнопки для продолжения

**Поведение:**
- Подсчитывает сумму баллов по всем элементам с ID, начинающимся с `prefix`
- Вычисляет процент ошибок: `(maxscore - score) / maxscore * 100`
- Выбирает сообщение из `score` по проценту ошибок (берется первое, где процент ошибок <= ключа)
- Подставляет `{score}` и `{maxscore}` в текст

**Пример:**

```yaml
Final_Test:
  type: test
  prefix: q_
  text: |
    *Ваша готовность к внедрению ИИ: {score} из {maxscore}.*
  score:
    20: Вы готовы запускать!
    65: У вас есть база, но стоит доработать детали.
    100: Притормозите запуск. Подготовка сейчас сэкономит время и деньги.
  button: OK
```

---

### Jump

Навигационный элемент с опциями для перехода к разным элементам или задержки.

**Параметры:**
- `type`: `jump` (обязательно)
- `text` (обязательно) - вопрос или сообщение
- `options` (опционально) - массив опций:
  - `text` (обязательно) - текст кнопки
  - `goto` (опционально) - ID элемента для перехода
  - `wait` (опционально) - задержка перед продолжением (формат: `1d:2h:3m:4s`)
  - `wait_text` (опционально) - сообщение при выборе опции с задержкой

**Поведение:**
- Если указан `goto`, переходит к указанному элементу
- Если указан `wait`, добавляет элемент в очередь ожидания на указанное время
- Если ни `goto`, ни `wait` не указаны, просто продолжает к следующему элементу

**Пример:**

```yaml
Continue_Question:
  type: jump
  text: Ну что, продолжим дальше?
  options:
    - text: Продолжим!
    - text: Давай завтра
      wait: 1d
      wait_text: Хорошо, вернемся к этому завтра!
    - text: Нет, хватит!
      goto: End_Message
```

---

### Revision

Повторение элементов, в которых были допущены ошибки.

**Параметры:**
- `type`: `revision` (обязательно)
- `text` (обязательно) - сообщение при наличии ошибок для повторения
- `prefix` (обязательно) - префикс ID элементов для поиска ошибок
- `no_mistakes` (обязательно) - сообщение, если ошибок нет
- `button` (опционально) - текст кнопки для продолжения

**Поведение:**
- Ищет все элементы с ID, начинающимся с `prefix`, где пользователь получил оценку 0
- Если есть ошибки, показывает `text` и создает цепочку элементов для повторения
- Если ошибок нет, показывает `no_mistakes`

**Пример:**

```yaml
Revision_Vocab:
  type: revision
  prefix: vocab_
  text: |
    Давай повторим слова, в которых ты ошибся:
  no_mistakes: |
    Отлично! Ты не допустил ошибок.
  button: Начать повторение
```

---

### Delay

Задержка перед переходом к следующему элементу. Элемент автоматически добавляется в очередь ожидания.

**Параметры:**
- `type`: `delay` (обязательно)
- `text` (опционально) - сообщение при установке задержки
- `wait` (обязательно) - время задержки (формат: `1d:2h:3m:4s`)
- `goto` (обязательно) - ID элемента для перехода после задержки

**Поведение:**
- Автоматически добавляет элемент в очередь ожидания
- После истечения времени `wait` автоматически переходит к элементу `goto`
- Не требует взаимодействия пользователя

**Пример:**

```yaml
Delay_01:
  type: delay
  text: Ты закончил модуль 1! Завтра в это же время прилетит новое задание!
  wait: 1d
  goto: Module_2_Start
```

```yaml
# Тихая задержка без сообщения
Delay_Silent:
  type: delay
  wait: 2h:30m
  goto: Next_Element
```

**Формат времени:**
- `30s` - 30 секунд
- `2m` - 2 минуты
- `1h:30m` - 1 час 30 минут
- `1d:12h` - 1 день 12 часов
- `1d:2h:3m:4s` - полный формат

---

### End

Завершает выполнение курса. После этого элемента никакие другие элементы не будут отправлены.

**Параметры:**
- `type`: `end` (обязательно)
- `text` (опционально) - финальное сообщение

**Поведение:**
- Отправляет финальное сообщение (если указано)
- Помечает курс как завершенный в базе данных
- Останавливает выполнение курса

**Пример:**

```yaml
Course_End:
  type: end
  text: |
    Спасибо за прохождение курса!
    Курс завершен. Дальнейшие элементы не будут отправлены.
```

```yaml
# Тихий конец без сообщения
Silent_End:
  type: end
```

---

### Section

Разделитель секций для организации структуры курса в редакторе. Этот элемент используется только для визуального разделения курса на секции и **игнорируется чатботом** при выполнении курса.

**Параметры:**
- `type`: `section` (обязательно)
- `title` (обязательно) - название раздела

**Поведение:**
- Элемент не отправляется пользователю и не обрабатывается чатботом
- Используется исключительно в редакторе курсов для организации структуры
- При выполнении курса чатбот автоматически пропускает этот элемент и переходит к следующему

**Пример:**

```yaml
Section_Intro:
  type: section
  title: Введение
```

```yaml
Section_Main:
  type: section
  title: Основная часть курса
```

**Примечание:** Элементы типа `section` служат только для визуальной организации курса в редакторе и не влияют на логику выполнения курса. Чатбот полностью игнорирует эти элементы при прохождении курса пользователем.

---

## Порядок выполнения элементов

Элементы выполняются последовательно в порядке их определения в YAML файле, если не указано иное:

- **Обычный порядок**: элементы выполняются один за другим
- **Jump с `goto`**: переход к указанному элементу
- **Delay**: переход к `goto` после задержки
- **End**: остановка выполнения курса

## Сохранение результатов

Все элементы автоматически сохраняют результаты в базу данных:
- Текст сообщений бота и ответы пользователя
- Оценки (score/maxscore) для элементов с проверкой
- История диалогов для Dialog элементов

Эти данные используются для:
- Генерации отчетов
- Подсчета результатов в Test
- Повторения ошибок в Revision
- Референсов в Dialog через `{{element_id}}`

