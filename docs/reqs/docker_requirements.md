# Требования: Запуск веб-приложения в Docker

**Версия:** 1.0  
**Дата:** 2024-12  
**Статус:** Требования  
**Приоритет:** Высокий

---

## Краткое резюме

Этот документ описывает требования к Docker-изации веб-приложения ProfoChatBot Web для обеспечения:

- **Консистентного окружения** для разработки и production
- **Простой установки и запуска** без необходимости ручной настройки зависимостей
- **Масштабируемости** и возможности горизонтального масштабирования
- **Изоляции** компонентов приложения
- **Безопасности** при развертывании

Веб-приложение состоит из трех основных компонентов:
- **Frontend** (Next.js 14, TypeScript)
- **Backend** (FastAPI, Python 3.12+)
- **Database** (PostgreSQL 14+)

---

## 1. Обзор

### 1.1 Текущее состояние

В проекте уже существует базовая Docker-изация:
- ✅ Dockerfile для backend (`webapp/backend/Dockerfile`)
- ✅ Dockerfile для frontend (`webapp/frontend/Dockerfile`)
- ✅ docker-compose.yml для локальной разработки (`webapp/docker-compose.yml`)
- ✅ Скрипты для управления Docker (`bin/webapp/docker-*.sh`)

### 1.2 Цели Docker-изации

1. **Упрощение развертывания** - один команда для запуска всего стека
2. **Консистентность окружения** - одинаковое поведение на всех машинах
3. **Изоляция зависимостей** - каждый компонент в своем контейнере
4. **Масштабируемость** - возможность запуска нескольких инстансов
5. **Production-ready** - готовность к развертыванию в production

---

## 2. Требования к Dockerfile

### 2.1 Backend Dockerfile

**Требования:**

1. **Базовый образ:**
   - Использовать официальный Python образ: `python:3.12-slim`
   - Минимальный размер образа для быстрой загрузки

2. **Рабочая директория:**
   - Установить `/app` как рабочую директорию

3. **Зависимости:**
   - Копировать `requirements.txt` перед кодом (для кэширования слоев)
   - Установить зависимости через `pip install --no-cache-dir`
   - Не устанавливать зависимости из venv

4. **Код приложения:**
   - Копировать весь код приложения
   - Включать все необходимые модули из корня проекта (course.py, db.py, elements/, etc.)

5. **Переменные окружения:**
   - Поддержка переменных окружения через ENV
   - Использовать переменные для конфигурации

6. **Порты:**
   - EXPOSE порт 8000 (стандартный для FastAPI)

7. **Команда запуска:**
   - Использовать `uvicorn` для запуска приложения
   - Для production: `uvicorn app.main:app --host 0.0.0.0 --port 8000`
   - Для development: `--reload` флаг (опционально)

8. **Пользователь:**
   - Запускать от непривилегированного пользователя (не root)

**Пример структуры:**
```dockerfile
FROM python:3.12-slim

WORKDIR /app

# Установка системных зависимостей (если нужны)
RUN apt-get update && apt-get install -y --no-install-recommends \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# Копирование и установка Python зависимостей
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# Копирование кода приложения
COPY . .

# Создание непривилегированного пользователя
RUN useradd -m -u 1000 appuser && chown -R appuser:appuser /app
USER appuser

EXPOSE 8000

CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 2.2 Frontend Dockerfile

**Требования:**

1. **Многоэтапная сборка (multi-stage build):**
   - **Stage 1 (build):** Сборка Next.js приложения
   - **Stage 2 (runtime):** Минимальный runtime образ

2. **Stage 1 - Build:**
   - Базовый образ: `node:20-alpine`
   - Установка зависимостей из `package.json`
   - Сборка приложения: `npm run build`
   - Копирование только необходимых файлов

3. **Stage 2 - Runtime:**
   - Базовый образ: `node:20-alpine`
   - Копирование только собранных файлов из Stage 1
   - Установка только production зависимостей (`npm ci --only=production`)

4. **Переменные окружения:**
   - Поддержка переменных окружения Next.js
   - Использование `NEXT_PUBLIC_*` для публичных переменных

5. **Порты:**
   - EXPOSE порт 3000

6. **Команда запуска:**
   - Для production: `npm start`
   - Для development: `npm run dev` (опционально)

**Пример структуры:**
```dockerfile
# Stage 1: Build
FROM node:20-alpine AS builder

WORKDIR /app

COPY package*.json ./
RUN npm ci

COPY . .
RUN npm run build

# Stage 2: Runtime
FROM node:20-alpine AS runner

WORKDIR /app

ENV NODE_ENV=production

# Создание непривилегированного пользователя
RUN addgroup --system --gid 1001 nodejs && \
    adduser --system --uid 1001 nextjs

# Копирование собранных файлов
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000

ENV PORT=3000

CMD ["node", "server.js"]
```

**Примечание:** Для использования standalone режима Next.js необходимо настроить `next.config.js`:
```javascript
module.exports = {
  output: 'standalone',
  // ...
}
```

### 2.3 Оптимизация Dockerfile

**Требования к оптимизации:**

1. **Кэширование слоев:**
   - Копировать файлы зависимостей перед кодом приложения
   - Использовать `.dockerignore` для исключения ненужных файлов

2. **Размер образа:**
   - Использовать alpine-образы где возможно
   - Очищать кэш пакетных менеджеров
   - Удалять временные файлы после установки

3. **Безопасность:**
   - Запускать от непривилегированного пользователя
   - Минимизировать количество установленных пакетов
   - Регулярно обновлять базовые образы

4. **.dockerignore файлы:**
   - Исключать `node_modules`, `venv`, `.git`, `.next`
   - Исключать файлы разработки (`.env.local`, `*.log`)
   - Исключать документацию и тесты (опционально)

---

## 3. Требования к docker-compose.yml

### 3.1 Базовая структура

**Требования:**

1. **Версия Compose:**
   - Использовать версию 3.8 или выше

2. **Сервисы:**
   - `db` - PostgreSQL база данных
   - `backend` - FastAPI backend
   - `frontend` - Next.js frontend

3. **Сети:**
   - Все сервисы в одной сети для коммуникации
   - Использование внутренних DNS имен

4. **Volumes:**
   - Постоянное хранилище для PostgreSQL данных
   - Опциональные volumes для разработки (код приложения)

5. **Зависимости:**
   - Backend зависит от db
   - Frontend зависит от backend

### 3.2 Конфигурация сервисов

#### 3.2.1 Database Service

**Требования:**

```yaml
db:
  image: postgres:14-alpine
  container_name: profochatbot_db
  environment:
    POSTGRES_USER: ${POSTGRES_USER:-profochatbot}
    POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-profochatbot}
    POSTGRES_DB: ${POSTGRES_DB:-profochatbot_web}
  ports:
    - "${POSTGRES_PORT:-5433}:5432"
  volumes:
    - postgres_data:/var/lib/postgresql/data
  healthcheck:
    test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-profochatbot}"]
    interval: 10s
    timeout: 5s
    retries: 5
  networks:
    - profochatbot_network
```

**Ключевые требования:**
- Использовать переменные окружения для конфигурации
- Настроить healthcheck для проверки готовности БД
- Использовать volume для персистентности данных
- Использовать alpine образ для меньшего размера

#### 3.2.2 Backend Service

**Требования:**

```yaml
backend:
  build:
    context: ./backend
    dockerfile: Dockerfile
  container_name: profochatbot_backend
  environment:
    DATABASE_URL: postgresql://${POSTGRES_USER:-profochatbot}:${POSTGRES_PASSWORD:-profochatbot}@db:5432/${POSTGRES_DB:-profochatbot_web}
    SECRET_KEY: ${SECRET_KEY}
    OPENAI_API_KEY: ${OPENAI_API_KEY}
    FRONTEND_URL: ${FRONTEND_URL:-http://localhost:3000}
    ENVIRONMENT: ${ENVIRONMENT:-development}
    BOT_API_TOKEN: ${BOT_API_TOKEN:-}
    BOT_NAME: ${BOT_NAME:-api_bot}
  ports:
    - "${BACKEND_PORT:-8000}:8000"
  depends_on:
    db:
      condition: service_healthy
  volumes:
    # Для разработки: монтирование кода
    - ./backend:/app
    # Для production: убрать volume
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    interval: 30s
    timeout: 10s
    retries: 3
  networks:
    - profochatbot_network
```

**Ключевые требования:**
- Использовать `depends_on` с `condition: service_healthy`
- Настроить healthcheck endpoint
- Переменные окружения через `.env` файл
- Volumes только для development режима

#### 3.2.3 Frontend Service

**Требования:**

```yaml
frontend:
  build:
    context: ./frontend
    dockerfile: Dockerfile
  container_name: profochatbot_frontend
  environment:
    NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:8000}
    NODE_ENV: ${NODE_ENV:-production}
  ports:
    - "${FRONTEND_PORT:-3000}:3000"
  depends_on:
    - backend
  volumes:
    # Для разработки: монтирование кода
    - ./frontend:/app
    - /app/node_modules
    - /app/.next
  networks:
    - profochatbot_network
```

**Ключевые требования:**
- Зависимость от backend сервиса
- Переменные окружения для API URL
- Volumes для hot-reload в development

### 3.3 Volumes и Networks

**Требования:**

```yaml
volumes:
  postgres_data:
    driver: local

networks:
  profochatbot_network:
    driver: bridge
```

**Ключевые требования:**
- Named volume для PostgreSQL данных
- Отдельная сеть для изоляции

### 3.4 Production vs Development

**Требования к разным окружениям:**

1. **Development (docker-compose.yml):**
   - Volumes для монтирования кода (hot-reload)
   - Reload режим для backend
   - Dev режим для frontend
   - Отладочные порты

2. **Production (docker-compose.prod.yml):**
   - Без volumes для кода
   - Production режим для всех сервисов
   - Оптимизированные образы
   - Healthchecks для всех сервисов
   - Restart policies

**Пример production конфигурации:**

```yaml
version: '3.8'

services:
  db:
    # ... как выше
    restart: unless-stopped
    
  backend:
    # ... как выше
    restart: unless-stopped
    # Без volumes для кода
    
  frontend:
    # ... как выше
    restart: unless-stopped
    # Без volumes для кода
```

---

## 4. Требования к переменным окружения

### 4.1 Backend переменные

**Обязательные переменные:**

```bash
# Database
DATABASE_URL=postgresql://user:password@db:5432/dbname

# Security
SECRET_KEY=your-secret-key-here

# External APIs
OPENAI_API_KEY=your-openai-api-key
BOT_API_TOKEN=your-telegram-bot-token

# Application
ENVIRONMENT=production|development
FRONTEND_URL=http://localhost:3000
BOT_NAME=api_bot
```

**Опциональные переменные:**

```bash
# Database connection pool
DB_POOL_SIZE=10
DB_MAX_OVERFLOW=20

# API settings
API_HOST=0.0.0.0
API_PORT=8000

# Logging
LOG_LEVEL=INFO
```

### 4.2 Frontend переменные

**Обязательные переменные:**

```bash
# API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Environment
NODE_ENV=production|development
```

**Опциональные переменные:**

```bash
# Analytics (если используется)
NEXT_PUBLIC_ANALYTICS_ID=...

# Feature flags
NEXT_PUBLIC_ENABLE_FEATURE_X=true
```

### 4.3 Database переменные

**Обязательные переменные:**

```bash
POSTGRES_USER=profochatbot
POSTGRES_PASSWORD=profochatbot
POSTGRES_DB=profochatbot_web
```

**Опциональные переменные:**

```bash
POSTGRES_PORT=5433
POSTGRES_INITDB_ARGS=--encoding=UTF8
```

### 4.4 Управление переменными окружения

**Требования:**

1. **Файлы .env:**
   - `.env.example` - пример конфигурации (в git)
   - `.env` - реальная конфигурация (в .gitignore)
   - `.env.production` - для production окружения
   - `.env.development` - для development окружения

2. **Безопасность:**
   - Никогда не коммитить реальные секреты
   - Использовать секреты из внешних систем (Docker secrets, Kubernetes secrets)
   - Ротация секретов

3. **Документация:**
   - Описание всех переменных в README
   - Примеры значений для каждой переменной

---

## 5. Требования к миграциям базы данных

### 5.1 Инициализация БД

**Требования:**

1. **Автоматическое применение миграций:**
   - При первом запуске backend контейнера
   - Использование Alembic для миграций
   - Скрипт инициализации в entrypoint

2. **Скрипт инициализации:**

```bash
#!/bin/bash
# wait-for-db.sh

set -e

host="$1"
shift
cmd="$@"

until psql -h "$host" -U "$POSTGRES_USER" -d "$POSTGRES_DB" -c '\q'; do
  >&2 echo "PostgreSQL is unavailable - sleeping"
  sleep 1
done

>&2 echo "PostgreSQL is up - executing command"
exec $cmd
```

3. **Entrypoint скрипт для backend:**

```bash
#!/bin/bash
set -e

# Ожидание готовности БД
./wait-for-db.sh db

# Применение миграций
alembic upgrade head

# Запуск приложения
exec "$@"
```

### 5.2 Версионирование миграций

**Требования:**

1. Все миграции должны быть в git
2. Миграции должны быть идемпотентными
3. Резервное копирование перед применением миграций (production)

---

## 6. Требования к безопасности

### 6.1 Образы

**Требования:**

1. **Базовые образы:**
   - Использовать официальные образы из Docker Hub
   - Регулярно обновлять базовые образы
   - Проверять образы на уязвимости (docker scan)

2. **Зависимости:**
   - Регулярно обновлять зависимости
   - Использовать фиксированные версии (не latest)
   - Проверять зависимости на уязвимости

3. **Пользователи:**
   - Запускать от непривилегированных пользователей
   - Не использовать root пользователя

### 6.2 Сети

**Требования:**

1. **Изоляция:**
   - Использовать отдельную сеть для приложения
   - Не экспортировать внутренние порты наружу (кроме необходимых)
   - Использовать reverse proxy для внешнего доступа

2. **Firewall:**
   - Ограничить доступ к портам БД только из backend контейнера
   - Использовать firewall правила в production

### 6.3 Секреты

**Требования:**

1. **Хранение секретов:**
   - Не хранить секреты в образах
   - Использовать переменные окружения или Docker secrets
   - Ротация секретов

2. **Доступ:**
   - Минимальные права доступа
   - Разделение секретов по окружениям

---

## 7. Требования к производительности

### 7.1 Оптимизация образов

**Требования:**

1. **Размер образов:**
   - Минимизировать размер образов
   - Использовать multi-stage builds
   - Очищать кэш после установки

2. **Слои:**
   - Оптимизировать порядок команд в Dockerfile
   - Группировать команды для лучшего кэширования

### 7.2 Ресурсы

**Требования:**

1. **Ограничения ресурсов:**

```yaml
services:
  backend:
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 1G
        reservations:
          cpus: '0.5'
          memory: 512M
```

2. **Мониторинг:**
   - Логирование использования ресурсов
   - Алерты при превышении лимитов

---

## 8. Требования к развертыванию

### 8.0 Скрипты управления из bin/

**Требования:**

Все операции с Docker должны быть доступны через скрипты из директории `bin/webapp/` для упрощения использования и консистентности.

1. **Сборка Docker образов:**
   - Должен существовать скрипт `bin/webapp/docker-build.sh` для локальной сборки образов
   - Скрипт должен собирать все необходимые образы (backend, frontend)
   - Поддержка опций для сборки конкретного сервиса
   - Поддержка флага `--no-cache` для полной пересборки

2. **Запуск через скрипты:**
   - Использование `bin/webapp/docker-start.sh` для запуска всех сервисов
   - Автоматическая проверка наличия Docker и Docker Compose
   - Автоматическая проверка и создание `.env` файла при необходимости

3. **Остановка через скрипты:**
   - Использование `bin/webapp/docker-stop.sh` для остановки сервисов

4. **Просмотр логов:**
   - Использование `bin/webapp/docker-logs.sh [service]` для просмотра логов

**Пример скрипта сборки (`bin/webapp/docker-build.sh`):**

```bash
#!/bin/bash

# Сборка Docker образов для веб-приложения
# Использование: ./bin/webapp/docker-build.sh [--no-cache] [backend|frontend|all]

PROJECT_ROOT="$( cd "$( dirname "${BASH_SOURCE[0]}" )/../.." && pwd )"
cd "$PROJECT_ROOT/webapp" || exit

NO_CACHE=""
SERVICE="all"

# Парсинг аргументов
while [[ $# -gt 0 ]]; do
    case $1 in
        --no-cache)
            NO_CACHE="--no-cache"
            shift
            ;;
        backend|frontend|all)
            SERVICE="$1"
            shift
            ;;
        *)
            echo "Неизвестный аргумент: $1"
            echo "Использование: $0 [--no-cache] [backend|frontend|all]"
            exit 1
            ;;
    esac
done

echo "🔨 Сборка Docker образов"
echo ""

if [ "$SERVICE" = "all" ] || [ "$SERVICE" = "backend" ]; then
    echo "📦 Сборка Backend образа..."
    docker-compose build $NO_CACHE backend
fi

if [ "$SERVICE" = "all" ] || [ "$SERVICE" = "frontend" ]; then
    echo "📦 Сборка Frontend образа..."
    docker-compose build $NO_CACHE frontend
fi

echo ""
echo "✅ Сборка завершена!"
```

**Требования к скриптам:**

1. **Единообразие:**
   - Все скрипты должны находиться в `bin/webapp/`
   - Единый стиль вывода сообщений
   - Обработка ошибок

2. **Проверки:**
   - Проверка наличия Docker и Docker Compose
   - Проверка наличия необходимых файлов
   - Валидация переменных окружения

3. **Документация:**
   - Комментарии в скриптах
   - Описание использования в README
   - Примеры использования

4. **Права доступа:**
   - Скрипты должны быть исполняемыми (`chmod +x`)
   - Документировать в README необходимость сделать скрипты исполняемыми

### 8.1 Локальная разработка

**Требования:**

1. **Сборка образов:**
   ```bash
   # Через скрипт (рекомендуется)
   ./bin/webapp/docker-build.sh
   
   # Или напрямую через docker-compose
   cd webapp
   docker-compose build
   ```

2. **Запуск:**
   ```bash
   # Через скрипт (рекомендуется)
   ./bin/webapp/docker-start.sh
   
   # Или напрямую
   cd webapp
   docker-compose up -d
   ```

3. **Логи:**
   ```bash
   # Через скрипт
   ./bin/webapp/docker-logs.sh [service]
   
   # Или напрямую
   docker-compose logs -f [service]
   ```

4. **Остановка:**
   ```bash
   # Через скрипт (рекомендуется)
   ./bin/webapp/docker-stop.sh
   
   # Или напрямую
   docker-compose down
   ```

5. **Очистка:**
   ```bash
   docker-compose down -v  # с удалением volumes
   ```

### 8.2 Production развертывание

**Требования:**

1. **Подготовка:**
   - Сборка production образов
   - Настройка переменных окружения
   - Настройка секретов

2. **Запуск:**
   ```bash
   docker-compose -f docker-compose.prod.yml up -d
   ```

3. **Мониторинг:**
   - Healthchecks для всех сервисов
   - Логирование в централизованную систему
   - Метрики производительности

4. **Обновление:**
   - Blue-green deployment
   - Rolling updates
   - Откат изменений

### 8.3 CI/CD интеграция

**Требования:**

1. **Сборка образов:**
   - Автоматическая сборка при коммите
   - Тегирование образов по версиям
   - Push в registry

2. **Тестирование:**
   - Запуск тестов в контейнерах
   - Интеграционные тесты

3. **Развертывание:**
   - Автоматическое развертывание после успешных тестов
   - Уведомления о статусе развертывания

---

## 9. Требования к документации

### 9.1 README

**Требования:**

1. **Инструкции по установке:**
   - Предварительные требования
   - Установка Docker и Docker Compose
   - Настройка переменных окружения

2. **Инструкции по запуску:**
   - Development режим (через скрипты из `bin/webapp/`)
   - Production режим
   - Troubleshooting
   - Описание всех доступных скриптов из `bin/webapp/`

3. **Скрипты управления:**
   - Описание `docker-build.sh` - сборка образов
   - Описание `docker-start.sh` - запуск сервисов
   - Описание `docker-stop.sh` - остановка сервисов
   - Описание `docker-logs.sh` - просмотр логов
   - Примеры использования каждого скрипта

4. **Архитектура:**
   - Описание компонентов
   - Схема взаимодействия
   - Порты и endpoints

### 9.2 Комментарии в коде

**Требования:**

1. Комментарии в Dockerfile
2. Описание переменных окружения
3. Примеры использования

---

## 10. Требования к тестированию

### 10.1 Функциональное тестирование

**Требования:**

1. **Тесты в контейнерах:**
   - Запуск unit тестов
   - Запуск integration тестов
   - E2E тесты

2. **Тестовые данные:**
   - Отдельная БД для тестов
   - Фикстуры для тестов

### 10.2 Нагрузочное тестирование

**Требования:**

1. Стресс-тесты API
2. Проверка производительности под нагрузкой
3. Оптимизация на основе результатов

---

## 11. Чеклист реализации

### 11.1 Dockerfile

- [ ] Backend Dockerfile оптимизирован
- [ ] Frontend Dockerfile с multi-stage build
- [ ] .dockerignore файлы созданы
- [ ] Непривилегированные пользователи настроены
- [ ] Healthchecks добавлены

### 11.2 docker-compose.yml

- [ ] Базовая конфигурация работает
- [ ] Production конфигурация создана
- [ ] Volumes настроены правильно
- [ ] Networks настроены
- [ ] Healthchecks для всех сервисов
- [ ] Restart policies настроены

### 11.2.1 Скрипты управления

- [ ] Скрипт `bin/webapp/docker-build.sh` создан
- [ ] Скрипт `bin/webapp/docker-start.sh` обновлен (уже существует)
- [ ] Скрипт `bin/webapp/docker-stop.sh` обновлен (уже существует)
- [ ] Скрипт `bin/webapp/docker-logs.sh` обновлен (уже существует)
- [ ] Все скрипты имеют права на выполнение
- [ ] Скрипты документированы в README

### 11.3 Переменные окружения

- [ ] .env.example создан
- [ ] Документация переменных
- [ ] Безопасное хранение секретов

### 11.4 Миграции

- [ ] Скрипт ожидания БД
- [ ] Entrypoint для автоматических миграций
- [ ] Тестирование миграций

### 11.5 Безопасность

- [ ] Образы обновлены
- [ ] Зависимости проверены
- [ ] Секреты не в образах
- [ ] Сети изолированы

### 11.6 Документация

- [ ] README обновлен
- [ ] Инструкции по развертыванию
- [ ] Troubleshooting guide

### 11.7 Тестирование

- [ ] Тесты запускаются в Docker
- [ ] CI/CD настроен
- [ ] Production развертывание протестировано

---

## 12. Примеры использования

### 12.1 Локальная разработка

**Рекомендуемый способ (через скрипты из bin/):**

```bash
# Клонирование репозитория
git clone <repo>
cd profochatbot

# Настройка переменных окружения (автоматически при первом запуске)
# Или вручную:
cp webapp/backend/.env.example webapp/backend/.env
# Редактирование webapp/backend/.env

# Сборка Docker образов
./bin/webapp/docker-build.sh

# Запуск всех сервисов
./bin/webapp/docker-start.sh

# Применение миграций
docker-compose -f webapp/docker-compose.yml exec backend alembic upgrade head

# Просмотр логов
./bin/webapp/docker-logs.sh
# Или конкретного сервиса:
./bin/webapp/docker-logs.sh backend

# Остановка
./bin/webapp/docker-stop.sh
```

**Альтернативный способ (напрямую через docker-compose):**

```bash
# Клонирование репозитория
git clone <repo>
cd profochatbot/webapp

# Настройка переменных окружения
cp backend/.env.example backend/.env
# Редактирование backend/.env

# Сборка образов
docker-compose build

# Запуск всех сервисов
docker-compose up -d

# Применение миграций
docker-compose exec backend alembic upgrade head

# Просмотр логов
docker-compose logs -f

# Остановка
docker-compose down
```

**Сборка конкретного сервиса:**

```bash
# Сборка только backend
./bin/webapp/docker-build.sh backend

# Сборка только frontend
./bin/webapp/docker-build.sh frontend

# Полная пересборка без кэша
./bin/webapp/docker-build.sh --no-cache
```

### 12.2 Production развертывание

```bash
# Настройка production переменных
cp backend/.env.production.example backend/.env.production
# Редактирование backend/.env.production

# Сборка production образов
docker-compose -f docker-compose.prod.yml build

# Запуск production сервисов
docker-compose -f docker-compose.prod.yml up -d

# Проверка статуса
docker-compose -f docker-compose.prod.yml ps

# Просмотр логов
docker-compose -f docker-compose.prod.yml logs -f
```

### 12.3 Обновление приложения

```bash
# Получение обновлений
git pull

# Пересборка образов
docker-compose -f docker-compose.prod.yml build

# Перезапуск сервисов
docker-compose -f docker-compose.prod.yml up -d

# Применение миграций (если есть)
docker-compose -f docker-compose.prod.yml exec backend alembic upgrade head
```

---

## 13. Troubleshooting

### 13.1 Частые проблемы

**Проблема:** Backend не может подключиться к БД

**Решение:**
- Проверить, что БД запущена: `docker-compose ps`
- Проверить DATABASE_URL в переменных окружения
- Проверить логи БД: `docker-compose logs db`

**Проблема:** Frontend не может подключиться к Backend

**Решение:**
- Проверить NEXT_PUBLIC_API_URL
- Проверить, что backend доступен: `curl http://localhost:8000/health`
- Проверить CORS настройки в backend

**Проблема:** Миграции не применяются

**Решение:**
- Проверить, что БД готова (healthcheck)
- Проверить логи backend: `docker-compose logs backend`
- Запустить миграции вручную: `docker-compose exec backend alembic upgrade head`

### 13.2 Полезные команды

**Через скрипты (рекомендуется):**

```bash
# Сборка образов
./bin/webapp/docker-build.sh [--no-cache] [backend|frontend|all]

# Запуск сервисов
./bin/webapp/docker-start.sh

# Просмотр логов
./bin/webapp/docker-logs.sh [service]

# Остановка сервисов
./bin/webapp/docker-stop.sh
```

**Напрямую через docker-compose:**

```bash
# Просмотр логов всех сервисов
cd webapp
docker-compose logs -f

# Просмотр логов конкретного сервиса
docker-compose logs -f backend

# Перезапуск сервиса
docker-compose restart backend

# Выполнение команды в контейнере
docker-compose exec backend bash
docker-compose exec backend alembic upgrade head

# Очистка всего (включая volumes)
docker-compose down -v

# Пересборка образа
docker-compose build --no-cache backend
```

---

## 14. Дальнейшее развитие

### 14.1 Планируемые улучшения

1. **Kubernetes поддержка:**
   - Helm charts
   - Kubernetes manifests
   - Ingress конфигурация

2. **Мониторинг:**
   - Prometheus метрики
   - Grafana дашборды
   - Логирование в ELK stack

3. **Автоматизация:**
   - CI/CD pipeline
   - Автоматические тесты
   - Автоматическое развертывание

4. **Масштабирование:**
   - Горизонтальное масштабирование backend
   - Load balancing
   - Database replication

---

## Заключение

Данный документ описывает полный набор требований для Docker-изации веб-приложения ProfoChatBot Web. Реализация этих требований обеспечит:

- Простое развертывание на любой платформе
- Консистентное окружение для разработки и production
- Готовность к масштабированию
- Безопасность и изоляцию компонентов

Все требования должны быть реализованы с учетом лучших практик Docker и безопасности.
