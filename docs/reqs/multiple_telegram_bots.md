# Исследование: Запуск множества Telegram приложений

## Введение

Данный документ описывает результаты исследования текущей архитектуры Telegram бота и возможностей для запуска множества Telegram приложений на одном сервере.

## Текущая архитектура

### Как работает текущее Telegram приложение

#### 1. Точка входа и инициализация

**Файл:** `main.py`

Текущая реализация использует:
- **Фреймворк**: aiogram 3.x (асинхронный Telegram Bot API)
- **Метод получения обновлений**: Polling (`dp.start_polling()`)
- **Токен бота**: Читается из переменной окружения `BOT_API_TOKEN`
- **Идентификация бота**: Используется глобальная переменная `globals.BOT_NAME`

```python
# main.py:32
BOT_API_TOKEN = os.environ.get('BOT_API_TOKEN')

# main.py:45
bot = Bot(token=BOT_API_TOKEN)
dp = Dispatcher()

# main.py:558
await dp.start_polling(bot)
```

#### 2. Конфигурация через аргументы командной строки

Система поддерживает запуск с указанием папки бота через аргумент командной строки:

```python
# main.py:564-566
if len(sys.argv) > 1:
    globals.BOT_FOLDER = sys.argv[1] + "/"
    # argv[1] используется для различения процессов ботов на одном сервере
```

Это позволяет:
- Разделять курсы по папкам (`scripts/{BOT_FOLDER}/courses.yml`)
- Запускать несколько экземпляров с разными конфигурациями
- Использовать один и тот же код для разных ботов

#### 3. Идентификация бота в базе данных

**Файл:** `globals.py`, `db.py`

```python
# globals.py
BOT_NAME = os.getenv('BOT_NAME', '')  # Telegram username для сохранения в БД
BOT_FOLDER = ""  # Подпапка в scripts. Если не пустая, должна заканчиваться на /
```

Бот идентифицируется в БД через:
- `BOT_NAME` - используется в таблице `run` (поле `botname`)
- `BOT_FOLDER` - определяет путь к курсам (`scripts/{BOT_FOLDER}/courses.yml`)

#### 4. Запуск через скрипты

**Файл:** `bin/telegram/run.sh`

Скрипт запуска:
- Активирует виртуальное окружение
- Загружает переменные из `.env`
- Проверяет наличие `BOT_API_TOKEN`
- Удаляет webhook (если установлен) перед запуском polling
- Передает `BOT_FOLDER` как аргумент командной строки

```bash
# bin/telegram/run.sh:70-74
if [ -n "$BOT_FOLDER_ARG" ]; then
    python main.py "$BOT_FOLDER_ARG"
else
    python main.py
fi
```

## Проблемы текущей архитектуры для множественных ботов

### 1. Глобальные переменные

**Проблема:** Использование глобальных переменных (`globals.BOT_NAME`, `globals.BOT_FOLDER`) создает конфликты при запуске нескольких ботов в одном процессе Python.

**Текущее решение:** Запуск разных процессов для каждого бота (через разные аргументы командной строки).

### 2. Единый токен из переменной окружения

**Проблема:** `BOT_API_TOKEN` читается из переменной окружения, что позволяет запустить только один бот на процесс.

**Текущее решение:** Разные `.env` файлы или разные переменные окружения для каждого процесса.

### 3. Polling vs Webhook

**Текущий метод:** Polling (`dp.start_polling()`)

**Ограничения:**
- Каждый бот создает отдельный процесс, постоянно опрашивающий Telegram API
- Увеличение нагрузки на сервер при росте количества ботов
- Менее эффективно для production окружения

**Альтернатива:** Webhook - более эффективный метод для множественных ботов.

## Возможные решения для запуска множества ботов

### Вариант 1: Множественные процессы (текущий подход)

**Описание:** Запуск отдельного процесса Python для каждого бота.

**Преимущества:**
- Простота реализации (уже работает)
- Изоляция процессов (сбой одного бота не влияет на другие)
- Легко масштабировать через systemd/supervisor
- Не требует изменений в коде

**Недостатки:**
- Высокое потребление памяти (каждый процесс ~50-100MB)
- Больше процессов для управления
- Менее эффективное использование ресурсов

**Реализация:**
```bash
# Запуск разных ботов в разных процессах
BOT_API_TOKEN=token1 BOT_NAME=bot1 python main.py folder1 &
BOT_API_TOKEN=token2 BOT_NAME=bot2 python main.py folder2 &
BOT_API_TOKEN=token3 BOT_NAME=bot3 python main.py folder3 &
```

**Рекомендации:**
- Использовать systemd unit files или supervisor для управления процессами
- Настроить логирование в отдельные файлы для каждого бота
- Мониторинг ресурсов (CPU, память) для каждого процесса

### Вариант 2: Webhook с единым сервером

**Описание:** Использование webhook вместо polling с единым HTTP сервером для всех ботов.

**Преимущества:**
- Один процесс для всех ботов
- Более эффективное использование ресурсов
- Масштабируемость (один сервер может обрабатывать множество ботов)
- Рекомендуемый подход для production

**Недостатки:**
- Требует рефакторинга кода
- Необходим HTTPS сервер (SSL сертификат)
- Ограничение портов Telegram: 443, 80, 88, 8443

**Архитектура:**

```
┌─────────────────────────────────────────┐
│     FastAPI/aiohttp Web Server          │
│                                         │
│  ┌───────────────────────────────────┐  │
│  │  Webhook Handler                 │  │
│  │  /webhook/{bot_token}            │  │
│  └───────────────────────────────────┘  │
│           │                              │
│  ┌────────▼──────────┐                  │
│  │  Bot Manager      │                  │
│  │  - Bot instances  │                  │
│  │  - Dispatchers    │                  │
│  └───────────────────┘                  │
└─────────────────────────────────────────┘
           │
           ▼
    ┌──────────────┐
    │  PostgreSQL  │
    └──────────────┘
```

**Реализация с aiogram:**

```python
from aiogram import Bot, Dispatcher
from aiogram.webhook.aiohttp_server import TokenBasedRequestHandler
from aiohttp import web

# Менеджер ботов
class BotManager:
    def __init__(self):
        self.bots = {}  # {token: Bot}
        self.dispatchers = {}  # {token: Dispatcher}
    
    def register_bot(self, token: str, bot_name: str, bot_folder: str):
        bot = Bot(token=token)
        dp = Dispatcher()
        
        # Настройка обработчиков для этого бота
        setup_handlers(dp, bot_name, bot_folder)
        
        self.bots[token] = bot
        self.dispatchers[token] = dp
    
    def get_dispatcher(self, token: str) -> Dispatcher:
        return self.dispatchers.get(token)

# Глобальный менеджер
bot_manager = BotManager()

# Регистрация ботов
bot_manager.register_bot(
    token="TOKEN1",
    bot_name="bot1",
    bot_folder="folder1"
)
bot_manager.register_bot(
    token="TOKEN2",
    bot_name="bot2",
    bot_folder="folder2"
)

# Handler для webhook
async def webhook_handler(request):
    token = request.match_info['bot_token']
    dispatcher = bot_manager.get_dispatcher(token)
    
    if not dispatcher:
        return web.Response(status=404)
    
    handler = TokenBasedRequestHandler(dispatcher)
    return await handler.handle(request)

# Настройка маршрутов
app = web.Application()
app.router.add_route('*', '/webhook/{bot_token}', webhook_handler)

# Установка webhook для каждого бота
async def setup_webhooks():
    for token, bot in bot_manager.bots.items():
        await bot.set_webhook(
            url=f"https://yourdomain.com/webhook/{token}",
            secret_token="your_secret_token"
        )
```

**Требования:**
- HTTPS сервер (nginx + SSL сертификат)
- Рефакторинг для поддержки множественных экземпляров Bot/Dispatcher
- Удаление глобальных переменных, переход на контекст бота
- Конфигурационный файл со списком ботов и их параметров

### Вариант 3: Гибридный подход (Polling + Webhook)

**Описание:** Поддержка обоих методов с возможностью выбора для каждого бота.

**Преимущества:**
- Гибкость (можно использовать polling для разработки, webhook для production)
- Постепенная миграция (можно мигрировать ботов по одному)
- Fallback механизм

**Реализация:**
```python
class BotRunner:
    def __init__(self, token: str, mode: str = "polling"):
        self.bot = Bot(token=token)
        self.dp = Dispatcher()
        self.mode = mode
    
    async def start(self):
        if self.mode == "polling":
            await self.dp.start_polling(self.bot)
        elif self.mode == "webhook":
            await self.setup_webhook()
    
    async def setup_webhook(self):
        # Настройка webhook
        pass
```

### Вариант 4: Контейнеризация (Docker)

**Описание:** Каждый бот запускается в отдельном Docker контейнере.

**Преимущества:**
- Полная изоляция
- Легкое масштабирование (docker-compose, Kubernetes)
- Упрощенное развертывание
- Версионирование (разные версии кода для разных ботов)

**Недостатки:**
- Дополнительные ресурсы на контейнеры
- Сложность управления множеством контейнеров

**Реализация:**
```yaml
# docker-compose.yml
version: '3.8'
services:
  bot1:
    build: .
    environment:
      - BOT_API_TOKEN=${BOT1_TOKEN}
      - BOT_NAME=bot1
      - BOT_FOLDER=folder1
    command: python main.py folder1
  
  bot2:
    build: .
    environment:
      - BOT_API_TOKEN=${BOT2_TOKEN}
      - BOT_NAME=bot2
      - BOT_FOLDER=folder2
    command: python main.py folder2
```

## Рекомендации по реализации

### Краткосрочное решение (быстрое внедрение)

**Вариант:** Множественные процессы с systemd/supervisor

**Шаги:**
1. Создать конфигурационный файл со списком ботов:
   ```yaml
   # bots_config.yaml
   bots:
     - name: bot1
       token_env: BOT1_TOKEN
       folder: folder1
       bot_name: bot1
     - name: bot2
       token_env: BOT2_TOKEN
       folder: folder2
       bot_name: bot2
   ```

2. Создать скрипт для запуска всех ботов:
   ```bash
   # bin/telegram/run-all.sh
   #!/bin/bash
   # Читает bots_config.yaml и запускает каждый бот в отдельном процессе
   ```

3. Настроить systemd unit files для каждого бота:
   ```ini
   # /etc/systemd/system/profochatbot-bot1.service
   [Unit]
   Description=ProfoChatBot - Bot1
   
   [Service]
   Type=simple
   User=botuser
   WorkingDirectory=/path/to/profochatbot
   Environment="BOT_API_TOKEN=token1"
   Environment="BOT_NAME=bot1"
   ExecStart=/path/to/venv/bin/python main.py folder1
   Restart=always
   
   [Install]
   WantedBy=multi-user.target
   ```

4. Улучшить логирование:
   - Отдельные лог-файлы для каждого бота
   - Единый формат логов с идентификатором бота

### Долгосрочное решение (масштабируемость)

**Вариант:** Webhook с единым сервером

**Шаги:**
1. **Рефакторинг архитектуры:**
   - Удаление глобальных переменных
   - Создание класса `BotContext` для хранения состояния бота
   - Передача контекста через все функции

2. **Создание Bot Manager:**
   ```python
   class BotContext:
       def __init__(self, bot_name: str, bot_folder: str, token: str):
           self.bot_name = bot_name
           self.bot_folder = bot_folder
           self.token = token
           self.bot = Bot(token=token)
           self.dp = Dispatcher()
           self.courses = None
   ```

3. **Рефакторинг обработчиков:**
   - Передача `BotContext` вместо использования глобальных переменных
   - Изменение всех функций для работы с контекстом

4. **Создание webhook сервера:**
   - FastAPI или aiohttp сервер
   - Маршрут `/webhook/{bot_token}`
   - Обработка обновлений через соответствующий Dispatcher

5. **Конфигурация:**
   ```yaml
   # bots_config.yaml
   webhook:
     url: "https://yourdomain.com/webhook"
     secret_token: "your_secret_token"
   
   bots:
     - name: bot1
       token: "${BOT1_TOKEN}"
       bot_name: bot1
       folder: folder1
     - name: bot2
       token: "${BOT2_TOKEN}"
       bot_name: bot2
       folder: folder2
   ```

6. **Миграция:**
   - Поддержка обоих методов (polling и webhook)
   - Постепенная миграция ботов на webhook
   - Мониторинг и тестирование

## Технические детали

### Ограничения Telegram Bot API

1. **Порты для webhook:** Только 443, 80, 88, 8443
2. **HTTPS обязателен:** Webhook требует SSL сертификат
3. **Rate limits:** Ограничения на количество запросов в секунду
4. **Один webhook на бот:** Нельзя установить несколько webhook для одного бота

### Изменения в коде

#### Текущие глобальные зависимости:

```python
# globals.py
BOT_NAME = os.getenv('BOT_NAME', '')
BOT_FOLDER = ""

# Используется в:
# - db.py: для сохранения botname в таблице run
# - course.py: для загрузки курсов из scripts/{BOT_FOLDER}/
# - main.py: для идентификации бота
```

#### Необходимые изменения:

1. **Замена глобальных переменных на контекст:**
   ```python
   # Вместо globals.BOT_NAME использовать bot_context.bot_name
   # Вместо globals.BOT_FOLDER использовать bot_context.bot_folder
   ```

2. **Изменение сигнатур функций:**
   ```python
   # Было:
   def create_run(course_id, username, chat_id):
       # использует globals.BOT_NAME
   
   # Стало:
   def create_run(course_id, username, chat_id, bot_context):
       # использует bot_context.bot_name
   ```

3. **Обновление всех вызовов:**
   - Передача `bot_context` через все функции
   - Или использование dependency injection

### База данных

**Текущая структура:**
- Таблица `run` содержит поле `botname` (из `globals.BOT_NAME`)
- Таблица `conversation` связана с `run` через `run_id`

**Изменения не требуются:** Структура БД уже поддерживает множественные боты через поле `botname`.

## Примеры конфигурации

### Конфигурация для множественных процессов

```yaml
# bots_config.yaml
bots:
  - name: "production_bot"
    token_env: "BOT_API_TOKEN"
    folder: ""
    bot_name: "profochatbot"
    log_file: "logs/bot_production.log"
  
  - name: "test_bot"
    token_env: "BOT_TEST_TOKEN"
    folder: "test/"
    bot_name: "test_bot"
    log_file: "logs/bot_test.log"
  
  - name: "demo_bot"
    token_env: "BOT_DEMO_TOKEN"
    folder: "demo/"
    bot_name: "demo_bot"
    log_file: "logs/bot_demo.log"
```

### Конфигурация для webhook

```yaml
# bots_config.yaml
webhook:
  base_url: "https://api.yourdomain.com"
  path: "/webhook"
  secret_token: "${WEBHOOK_SECRET_TOKEN}"
  ssl_cert_path: "/path/to/cert.pem"

bots:
  - name: "production_bot"
    token: "${BOT_API_TOKEN}"
    bot_name: "profochatbot"
    folder: ""
    webhook_url: "${WEBHOOK_BASE_URL}/webhook/${BOT_API_TOKEN}"
  
  - name: "test_bot"
    token: "${BOT_TEST_TOKEN}"
    bot_name: "test_bot"
    folder: "test/"
    webhook_url: "${WEBHOOK_BASE_URL}/webhook/${BOT_TEST_TOKEN}"
```

## Мониторинг и управление

### Метрики для отслеживания

1. **Производительность:**
   - Время обработки сообщений
   - Количество активных пользователей на бот
   - Использование памяти и CPU

2. **Надежность:**
   - Количество ошибок
   - Время работы без перезапуска
   - Статус webhook (для webhook режима)

3. **Бизнес-метрики:**
   - Количество запущенных курсов
   - Активность пользователей
   - Конверсия по курсам

### Инструменты

- **Логирование:** Структурированные логи с идентификатором бота
- **Мониторинг:** Prometheus + Grafana для метрик
- **Алерты:** Уведомления при сбоях ботов
- **Управление:** CLI инструмент для управления ботами (start/stop/restart)

## Заключение

### Текущее состояние

Система уже поддерживает запуск множественных ботов через:
- Разные процессы (аргумент командной строки `BOT_FOLDER`)
- Разные переменные окружения (`BOT_API_TOKEN`, `BOT_NAME`)
- Разделение курсов по папкам

### Рекомендации

1. **Краткосрочно:** Использовать множественные процессы с systemd/supervisor для управления
2. **Долгосрочно:** Мигрировать на webhook с единым сервером для лучшей масштабируемости
3. **Постепенная миграция:** Поддержка обоих методов во время перехода

### Приоритеты

1. ✅ **Низкий приоритет:** Улучшение управления множественными процессами (systemd configs)
2. ⚠️ **Средний приоритет:** Рефакторинг для удаления глобальных переменных
3. 🚀 **Высокий приоритет:** Реализация webhook сервера для production масштабирования

---

**Дата создания:** 2024
**Автор:** AI Assistant
**Статус:** Исследование завершено, готово к обсуждению
